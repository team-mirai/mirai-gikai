# コードレビューガイド

Claude がコードレビューを行う際のガイドライン。
コーディング規約・テストルール・PR運用の詳細は [AGENTS.md](AGENTS.md) を参照すること。

## レビュー言語

レビューコメント・指摘・サマリーはすべて **日本語** で記述する。

## レビューの流れ

1. **差分の把握**: 変更ファイル一覧とコミット履歴から、変更の目的とスコープを理解する
2. **規約チェック**: AGENTS.md の各セクションに照らして違反がないか確認する
3. **ロジックレビュー**: 正確性・エッジケース・セキュリティの観点で精査する
4. **テストレビュー**: テストの有無・網羅性・品質を確認する
5. **総合判定**: 重要度付きの指摘一覧と、マージ可否を出力する

## チェック観点

### 1. AGENTS.md 規約準拠

以下の規約違反は **必ず指摘** する。詳細は [AGENTS.md](AGENTS.md) の該当セクションを参照。

| 観点 | AGENTS.md セクション | よくある違反例 |
|------|---------------------|---------------|
| Feature 構成 | Next.js アーキテクチャ指針 | page.tsx にロジック直書き、server/client 境界違反 |
| UI 規約 | Coding Style & Naming Conventions | `<button>` 直書き、インライン SVG、インラインカラーコード |
| ルート定義 | admin/web 内部ルート定義 | 文字列リテラルでのルート直書き |
| テスト | Testing Guidelines | 純粋関数のテスト漏れ、`vi.mock` 使用、DB function テスト漏れ |
| コード共有 | Project Structure | web/admin 間のコード重複（`packages/` 未使用） |
| 命名 | Coding Style & Naming Conventions | ファイル名がケバブケースでない、コンポーネントがパスカルケースでない |

### 2. ロジック・正確性

- 意図した動作が正しく実装されているか
- エッジケース（空配列、null、境界値）の考慮
- 既存機能への破壊的変更がないか
- N+1 クエリや不要な再レンダリングなどのパフォーマンス問題

### 3. セキュリティ

- SQL インジェクション、XSS、CSRF
- 機密情報（API キー、トークン）のハードコード
- RLS が有効化されているか（マイグレーション変更時）
- `createAdminClient()` の適切な使用

### 4. 可読性・保守性

- 関数の長さ（50行超は分割を検討）
- ネストの深さ（3段超は要注意）
- 命名の明確さ（`tmp`, `data` 等の汎用名を避ける）
- `any` 型、不必要な型アサーション（`as`）、non-null assertion（`!`）
- 空の catch ブロックによるエラー握りつぶし

### 5. テスト

テストルールの詳細は [AGENTS.md](AGENTS.md) の Testing Guidelines および [テストガイドライン](docs/20260219_1000_テストガイドライン.md) を参照。

- `utils/` 配下の新規純粋関数にテストがあるか
- `vi.mock("server-only")` 等のモックを使っていないか
- DB function の追加・変更に統合テストがあるか
- テストが実装の意図を正しく検証しているか（実装の詳細でなく振る舞いをテスト）

### 6. PR スコープ

- 現在のタスクに無関係な変更が混入していないか
- ついでのリファクタ・フォーマット修正が含まれていないか

## 指摘の重要度

| レベル | ラベル | 意味 | 対応 |
|--------|--------|------|------|
| Critical | `[Critical]` | セキュリティ脆弱性、データ損失リスク、本番障害の可能性 | **修正必須** |
| Major | `[Major]` | バグ、規約の重大な違反、破壊的変更 | **修正必須** |
| Minor | `[Minor]` | 改善推奨だが動作に支障なし。可読性・保守性の向上 | 修正推奨 |
| Nitpick | `[Nit]` | 好みレベルの細かい指摘 | 対応任意 |

## 出力フォーマット

```markdown
## レビュー結果

### 概要
<変更内容の要約を1-3行で>

### 指摘事項

#### [Critical/Major] タイトル
- **ファイル**: `path/to/file.ts:行番号`
- **内容**: 何が問題か
- **理由**: なぜ問題か
- **提案**: どう修正すべきか

#### [Minor] タイトル
- **ファイル**: `path/to/file.ts:行番号`
- **内容**: ...

#### [Nit] タイトル
- ...

### 良い点
- <良い実装や判断があれば記載>

### 総合判定
- **Critical**: N件 / **Major**: N件 / **Minor**: N件 / **Nit**: N件
- **判定**: マージ可 / 軽微な修正後マージ可 / 要修正
```

## 判定基準

- **マージ可**: Critical・Major が 0件
- **軽微な修正後マージ可**: Critical が 0件、Major が 1-2件で修正が軽微
- **要修正**: Critical が 1件以上、または Major が多数
